<h1>repro: transformIndexHtml is bypassed by SvelteKit dev</h1>
<p>
  If <code>DevTools()</code> were running, you would see the Vite DevTools
  drawer pinned to the edge of the viewport. There is no drawer here.
</p>
